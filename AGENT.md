# CartridgeFlow Agent Notes

先看 `TODO.md`。现在项目只需要轻量收敛，不要再把 TODO 写成流程制度。

## 工作方式

- TODO 只记录要干的事，短一点。
- 没在 TODO 里的新改动，先补一条简短 TODO。
- 一次只解决一个明确问题。
- 改完用 Git 提交，出问题靠时间线回滚。
- 不再单独建立“快照任务”。
- 不做无关重构，不批量重写 UI，不随手清理无关文件。

## 协议红线

- 已发布协议版本不要改。
- 协议语义需要变化时，先和用户讨论，再新建版本。
- 不要为了单个卡带让协议或基座能力漂移。

## 修改前

- 看 `git status`。
- 读相关文件。
- 确认当前 TODO 里有这件事。

## 提交前

- 确认只改了本次任务相关文件。
- 不提交依赖、缓存、日志、运行产物、密钥。
- 提交信息写清楚这次干了什么。

## 当前重点

1. 测试台 UI 回收：日志栏、详情面板、画布交互。
2. AI 决策真实闭环：错误细节、用户干预、结果消费。
3. `dev.pixel_episode_director` v3.0：用户和 AI 协作完成作品。
