# CartridgeFlow Goal

CartridgeFlow 的目标是成为一个可验证、可移植、可交互的 AI 工作流基座：

- 流程语义由版本化协议定义，而不是由某张卡带反向决定。
- AI 决策、工具副作用、用户输入、恢复和交付都有结构化契约。
- 业务卡带可以独立安装、停用和卸载，不在基座留下领域代码或运行残留。
- 开发测试不会伪装成真实生产能力。
- 发布版本可从空卡带状态启动，并能由新开发者或 AI 从文档独立接手。

当前基座版本：`v0.1.0`。
