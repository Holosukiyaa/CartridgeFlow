# CartridgeFlow Protocol-First Plan

状态：最小纵向闭环已经通过真实签名包运行验收，2026-08-05。

## 目标

恢复 CartridgeFlow 最初的组合优势：卡带既是完整应用，也是流程内部可复用的执行能力。意图编排层保留用户完整想法，不再因为官方没有某个适配器而拒绝生成，也不在打包时凭空猜测能力实现层事实。

```text
用户想法
  -> 意图编排层发现方向并形成可审核语义链路
  -> 每个语义节点解析到不可变可信能力发布物
  -> 缺失能力保留在原节点
  -> 能力卡带工坊补齐并发布特殊能力
  -> 原节点重新解析
  -> 创作空间审核
  -> 递归打包为完整应用卡带
  -> demos 独立验证和运行
```

## 产品边界

### 意图编排层：创作空间

创作空间包含“方向探索”和“方案编排”两种模式，只拥有语义事实：整体草稿、节点目标、可调整字段、来源审核和确认状态。它不显示 Root Flow、固定节点工具栏、MCP/模型绑定、权限、调优版本、运行按钮或调试台。

没有实现的合理需求显示为“待补齐能力”。它可以继续被编辑和持久化，只阻止依赖真实执行的最终打包。未解析需求可以被用户确认，但只有已经解析并审核的能力节点计入打包就绪数。

AI 给出的能力匹配只是待审核提议。用户可以在节点内选择“不适合当前节点”，原节点立即退回“待补齐能力”并保留完整需求；被拒绝的只是一份精确发布摘要，新版本仍可重新提议。

### 能力实现层：能力工坊

`/capabilities` 是吸收旧画布使用经验后的能力实现工作区。高级用户在同一张技术画布上创建内部 Flow、声明公开输入输出和创作空间可编辑字段、选择依赖、检查能力边界、验证成功与失败路径，并发布不可变的工作区可信能力。

从创作空间的能力缺口进入时，工坊带入目标、项目和原节点身份。发布后创作空间自动重新解析同一个节点，不要求用户重建流程。

工作区信任不能授予空壳：成功路径至少包含一个真实执行状态；工具或远程节点必须有可调用绑定；每个公开输出必须由成功路径显式产出。只创建“开始 -> 完成”或放置未配置节点时，发布按钮保持禁用。

### demos

`demos/` 是独立运行测试台。它消费能力候选物或最终签名包并提供执行证据，不读取创作空间会话，也不向创作空间塞回运行、暂停、历史或调试界面。

## 协议模型

- `CF-TUNING@1.5`：语义配方、能力匹配、不可变能力发布、公开端口、创作空间字段、信任和依赖。
- `CF-FARP@1.7`：递归依赖闭包、确定性命名空间、完整来源链和自包含 Root Flow 物化。
- `CF-CRE@1`：内容清单、签名和最终应用发布边界。

能力发布可以包含完整 Root Flow 和包自有 DLC，并固定所有子能力的精确版本与摘要。依赖是有序可执行前置能力；共享的传递依赖在一个语义节点实例内只物化一次。循环、悬空版本、摘要漂移、信任越界、公开端口不兼容、多成功出口和危险路径全部失败关闭。

Base 只提供协议、执行安全、broker 和 DLC 宿主，不内置 RSS 或任何业务领域能力。RSS 是 `demos/capabilities/rss-reader/` 中的示例能力卡带。

## 信任与打包

本地工坊只能发布“当前工作区可信”能力。系统可信和组织可信必须来自其各自治理入口，不能由本地 UI 自行授予。AI 可以辅助制作，但不能授予信任。

创作空间的“打包”是唯一能力实现映射出口。打包必须同时满足：

1. 所有必需语义节点已经解析到精确能力发布物；
2. 所有已解析能力已被用户审核；
3. 创作空间字段类型与目标参数完全匹配；
4. 依赖闭包、信任、公开端口、权限和路径校验通过；
5. 子 Flow 与包自有 DLC 被确定性物化；
6. 最终包保留语义节点、能力版本、摘要、依赖和源码来源；
7. 清单验证和签名成功。

## 已完成范围

- [x] 清理意图编排层中的能力实现节点工具栏、运行调试和调优概念。
- [x] 删除重复且停用的旧版独立创作前端工程。
- [x] 同一 FastAPI/8765 提供创作空间与能力工坊，不再绑定两个前端端口。
- [x] 创作空间在无可信能力时仍生成完整语义草稿并保留原节点。
- [x] 工坊支持真实 Flow 画布编辑、节点属性、公开端口、字段、依赖和边界检查。
- [x] 工坊发布完整 Flow 为不可变工作区可信能力。
- [x] 创作空间自动和手动重新解析，保留语义节点身份。
- [x] 创作空间可拒绝错误能力匹配，按节点和发布摘要防止原版本自动绑回。
- [x] 工坊阻止空 Flow、未绑定工具节点和未真实产出的公开输出获得工作区信任。
- [x] 递归打包固定版本、验证类型、命名空间化状态/存储/文件并合并 DLC。
- [x] 新增 RSS 包自有 DLC 示例，Base 不拥有 RSS 业务实现。
- [x] Base 提供透明 DLC 声明图执行器和受限 HTTPS broker，DLC 不能绕过声明直接使用网络能力。
- [x] 创作空间组合包生成完整资产注册表、CF-TUNING@1.5 绑定、递归存储键和最终 `handoff` 交付。
- [x] `demos` 通过公共导入与运行 API 消费最终签名包，不依赖创作空间会话。
- [x] 启动脚本构建两个界面并托管在同一服务。
- [x] 协议治理、单元/集成、前端构建、浏览器桌面和移动端纵向闭环验收。

## 闭环验收

闭环分为两项独立证据，不能再用“能生成签名包”代替“能运行”：

1. 语义创作闭环：已用系统预先不存在的“量子苔藓折叠校准”题材完成真实浏览器仿真。创作空间保留未解析节点；从节点进入工坊；创建并编辑内部 Flow；发布工作区能力；返回创作空间后原节点自动解析；审核后生成签名包。
2. 真实运行闭环：`demos/runtime-developer-toolkit/demo/live_creator_rss.py` 从临时创作空间会话生成 CF-CRE 签名包，经 `/api/cartridges/import` 激活，再由 `/api/cartridge-runs` 运行包内 RSS DLC。2026-08-05 使用 `https://github.blog/feed/` 得到 5 条真实条目，运行 `completed`、数据链 `passed=true`、交付 `delivered`；另一个绑定 `http://127.0.0.1/private-feed.xml` 的签名包按预期 `failed`。
3. 用户纠错闭环：已用真实浏览器让已配置 LLM 生成 5 节点“AI 日报”草稿，发现“外部信息获取”被错误提议为“AI 内容处理”后，从节点内退回为能力缺口；待补齐数从 2 变 3，原版本未自动绑回，需求上下文完整进入能力工坊。工坊中的空 Flow 和默认未配置远程节点均被禁止发布。

可重复执行的最近一次证据写入 `.data/reports/creator-live-closure.json`。验收脚本结束后删除测试运行和测试安装包，只保留报告与 worker 审计记录。

这证明的不是某一种 RSS 模板，而是“任意语义缺口 -> 自定义完整 Flow 能力 -> 发布 -> 原位复用 -> 递归应用打包 -> 独立 Base 运行与交付”的通用闭环。以后新增能力必须同时提供成功路径的非空真实输出和失败路径证据。

## 明确不做

- 不在创作空间恢复运行、测试、暂停、历史或调试台。
- 不在创作空间暴露能力实现协议字段或固定节点库。
- 不把全部业务能力写进 Base。
- 不让未验证候选能力进入最终包。
- 不维护两个互相调用的前端服务或端口。
- 不把整个应用藏成一个无法审核的万能能力节点。
