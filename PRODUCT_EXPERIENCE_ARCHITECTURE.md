# CartridgeFlow 产品体验架构

## 当前决策

CartridgeFlow Creator 是一个从模糊想法走到可交付配方的单画布产品。用户
始终面对同一张动态链路图，不需要理解节点实现，也不在多个前端之间搬运
项目身份或配置。

```text
第一层：还没有明确方案
  AI 帮用户发现方向
          ↓
第二层：已有目标或整体草稿
  AI 编排链路，用户调整整体并深入审核节点
          ↓
打包：唯一的第三层边界
  后台严格映射并签名
          ↓
demos：独立运行测试台
```

第三层是必要的执行事实，不是 Creator 的用户界面。Creator 中不得出现
Developer 页面、工程模式、运行台、调试、调优、版本选择或相关设置开关。

## 第一层：打开思维

用户可以只表达兴趣、困扰、场景或模糊偏好。AI 返回真实生成、可选择的
方向，说明结果、适用原因和需要确认的问题。方向不是写死模板，也不会声称
已经验证来源或执行能力。

用户也可以跳过方向发现，直接描述明确目标并生成整体草稿。

## 第二层：整体草稿与节点深化

AI 只能使用已经发布的可信能力编排动态链路，不能临时发明执行节点。整体
草稿中的每个节点默认待审核，带有第一次生成的 Creator 安全字段。

用户有两种互斥上下文：

1. 在画布下方继续和 AI 调整整个草稿；
2. 点击一个节点，在画布上方的单一编辑层中修改开放字段、请求 AI 深化、
   检查影响并确认节点。

用户不能从固定节点栏放置节点，不能手工建立工程连线，也看不到执行器、
权限、模型、工具、端点、映射键或协议字段。前端只呈现一个自动保存的当前
草稿，不提供修订历史、发布版本、回滚或调优概念。

## 可信能力

Developer 的职责是事先发布可复用的可信节点及其不可变执行映射。发布动作
完成执行授权；Creator 只能读取去除工程事实后的安全投影，并用它们组成配方。

能力不足时，Creator 只说明还缺少什么。它不打开 Developer 页面，也不把
工程配置表单嵌入当前画布。可信能力的生产和治理是独立维护流程，不是每个
Creator 项目的必经页面。

## 打包边界

“打包”是 Creator 唯一可以触发第三层映射的操作。后台在一个原子边界内：

- 校验当前草稿和关系；
- 校验所有节点已经审核；
- 校验每个节点固定到真实发布的可信能力和完整映射快照；
- 将 Creator 允许的值应用到 Developer 发布的执行模板；
- 校验生成的 CF-FARP Root Flow；
- 构建 CF-CRE 包并独立验证签名；
- 只向 Creator 返回下载所需的安全元数据。

Creator 不提交编译候选，不逐项目进入 Developer 确认，也不接收 Root Flow、
映射沿袭或工程拓扑。任何校验失败都不得产生包。

## 运行与测试

`demos/` 是包的独立运行测试台。Creator 不安装、不运行、不暂停、不停止、
不查看运行历史或调试事件。测试台只消费签名包，不读取 Creator 会话和前端
状态。

## 工程边界

- `src/frontend` 是当前 Creator 产品前端，只包含第一、二层和打包入口。
- `src/core/studio` 保存 Creator 会话、可信节点安全投影和严格打包服务。
- `src/core/lab`、运行器和工程协议仍可被可信能力发布、打包校验和 `demos/`
  使用，但不能成为 Creator 组件依赖。
- `src/creator-studio` 与 `src/developer-console` 是未启动的历史参考，不是当前
  产品路由，也不能反向决定 Creator 的信息架构。

任何新组件、API 或文案都必须先归属第一层、第二层或打包边界。无法明确
归层的功能不得进入 Creator。
