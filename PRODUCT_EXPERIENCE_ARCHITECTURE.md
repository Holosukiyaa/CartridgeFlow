# CartridgeFlow 产品体验架构

## 决策

CartridgeFlow 不是一个让所有人直接配置节点、工具和运行参数的
工作流编辑器。它要让用户从模糊想法出发，形成自己认可的配方，再将
配方可靠地交付为可验证的执行能力。

因此，产品必须按用户所处的认知阶段分层，而不是按旧 Workbench 的
技术模块组织页面。

```text
第一层：打开思维
第二层：形成并审核配方
第三层：实现、验证和交付
```

## 三层体验

### 第一层：打开思维

用户还没有明确要做什么。他可以表达兴趣、困扰、场景、已有材料或
模糊偏好，但不需要提出一个技术任务。

系统的承诺是帮助用户发现值得做的方向，而不是要求他填写 Flow、RSS、
模型或工具配置。第一层应给出可选择的可能性配方，并说明：

- 它能带来什么结果。
- 为什么可能适合用户。
- 第一周可能得到什么。
- 还需要用户确认什么。

`cartridgeflow-flow-author` 的可行性知识在这里作为后台能力使用：它
保证提出的方向有机会被后续编译为可靠流程，但绝不向用户暴露状态、
端口、工具 ID、模型绑定或协议字段。

### 第二层：形成并审核配方

用户已经知道想得到什么，但不知道应从哪里获取信息、应选什么资源、
或应如何组织步骤。

例如，用户希望持续得到某类 AI 行业信息，却不知道哪些网站提供合适的
RSS。系统应发现候选来源，并在画布上以可审核对象展示它们。每个来源
至少应让用户理解：它是什么、提供什么、为什么适合、近期内容是否符合
预期，以及是否愿意采用它。

可信节点管理属于本层。节点可以复用或新增，但用户只关心它在配方中
提供的能力和结果，不关心节点内部如何搭建。所有变更必须经过：

```text
proposal -> preview -> partial accept / reject -> freeze
```

画布表达的是用户可理解的步骤、来源、关系、风险和待确认项，而不是
执行端口图。

### 第三层：实现、验证和交付

被确认的配方由 Flow Author 和后端物化为可执行的 Root Flow。工程用户
在此阶段处理资源绑定、模型与工具、输入输出契约、失败路径、协议兼容、
调参、预检、测试证据和签名交付。

开发验证包括受控输入、mock 或受控真实资源、节点轨迹、暂停交互、失败
原因、数据链和签名验证。它不是生产 Runtime；生产安装、队列、执行、
历史和产物归外部 Runtime 所有。

## 两个前端工程

### Creator Studio

Creator Studio 服务第一、二层。它从探索开始，帮助用户选择方向、建立
配方、管理可信节点和来源、审核 AI 建议、冻结设计，并显示可理解的交付
摘要。

Creator Studio 不显示或操作 Root Flow、端口、模型绑定、MCP/DLC 工具、
凭据、协议版本、运行日志、测试输入、检查点、队列或生产执行。

### Developer Console

Developer Console 服务第三层。它接收已确认的配方及其物化结果，处理工程
配置和开发验证，并查看签名交付事实。

Developer Console 不读取 Creator 的私有探索内容、未采纳方向、提示词或
私有来源上下文。它只能向 Creator 回流可理解的澄清项或交付状态。

## 一个项目，一条旅程

用户不应在不相关的系统之间复制会话 ID、Flow ID 或本地路径。系统必须
维护同一个项目的稳定身份链：

```text
项目
  -> 创作会话 / 配方修订
  -> 编译候选
  -> 工程物化 Root Flow
  -> 验证记录
  -> 已签名交付包
```

正常用户路径只有一个主要切换点：Creator 中的配方审核完成后，进入同一
项目对应的 Developer Console 工程验证工作区。

```text
Creator Studio
  探索 -> 配方 -> 审核 -> 冻结
  -> 进入工程验证
Developer Console
  物化 -> 配置 -> 开发验证 -> 签名交付
External Runtime
  安装与生产执行
```

部署时，Creator、Developer 与旧 Workbench 必须由同一产品域名和同一 API 提供服务。
它们可以是独立代码包和静态构建产物，但只能作为路由域出现，例如 `/creator/`、
`/developer/` 和兼容 Workbench 路由。项目内的工作地址必须是
`/projects/:projectId/creator` 与 `/projects/:projectId/developer`；不得将端口、
跨前端本地状态或手工复制 ID 作为用户交接机制。

若工程验证发现缺少业务决定，Developer Console 必须创建自然语言的澄清
项回流 Creator。Creator 确认后产生新配方修订，旧候选失效并重新物化、
验证；不得把技术错误直接抛给创作者。

## 旧 Workbench 的定位

旧 `src/frontend` 是第三层能力的完整兼容实现和迁移参考，尤其保留既有
Test Bench、历史协议和回归验证价值。它不属于普通用户的目标路径。

测试台应逐项迁入 Developer Console 的“开发验证”视图，迁移完成前仅供
内部工程人员处理尚未覆盖的能力。不得将测试台、运行控制或工程诊断加入
Creator Studio。

## 实施规则

- Creator Studio 先完成功能与语义结构，不引入最终视觉皮肤、通用 UI
  框架或工程画布依赖；视觉层在功能稳定后独立覆盖。
- 所有 Creator 操作使用创作者语言和稳定的无障碍标签；不泄露技术操作名。
- 前后端共享的是受控投影和稳定项目身份，不共享整页状态或私有数据。
- Flow Author 是“配方到 Flow”的内部编译器，不是 Creator 用户界面。
- 一个 Creator 设计修改必须使受影响的工程候选和验证结果显式过期。
- 任何新页面或 API 都必须明确归属第一、二或第三层；跨层数据必须经过
  投影和审查。
