# 智能焦点导航器设计

## 🎯 设计理念

### 核心原则
1. **突出重点** - 只显示需要用户注意的节点
2. **可扩展** - 支持 100+ 节点而不会撑爆界面
3. **视觉冲击** - 用颜色、动画、图标强调紧急任务
4. **信息完整** - 压缩显示整体进度统计

---

## 🧠 智能过滤逻辑

### 当节点数 ≤ 5
**显示全部节点** - 信息量不大，无需折叠

### 当节点数 > 5
**只显示待处理节点**（最多 4 个）：
- ❌ **待补齐** (is-unresolved) - 最高优先级
- 🔵 **待审核** (is-review) - 次优先级
- ✅ **已确认** (is-trusted) - 如果不是当前选中节点则隐藏
- 📍 **当前选中** - 始终显示（即使已确认）

### 折叠信息
显示压缩统计卡片：
```
[ 23已确认 | 7待处理 | 共 30 步 ]
```

---

## 🎨 视觉设计

### 1. 待补齐节点 (is-unresolved)
```css
背景: 渐变（浅橙 → 白）
边框: 2px 红色，左侧 4px
动画: 脉冲呼吸效果（2秒循环）
角标: 红色圆圈 "!" 徽章
文字: 红色加粗
```

**视觉示意**:
```
╔═══════════════════════════╗ ⚠️
║ !  03  能力缺口           ║
║     待补齐                ║ ← 脉冲阴影
╚═══════════════════════════╝
    ↑ 4px 红色左边框
```

**动画效果**: 
每 2 秒从节点边缘向外扩散一圈红色阴影，吸引注意力。

---

### 2. 待审核节点 (is-review)
```css
背景: 渐变（浅蓝 → 白）
边框: 2px 蓝色，左侧 4px
文字: 蓝色加粗
```

**视觉示意**:
```
╔═══════════════════════════╗
║   04  结果呈现            ║
║      待审核               ║
╚═══════════════════════════╝
    ↑ 4px 蓝色左边框
```

---

### 3. 已确认节点 (is-trusted)
```css
背景: 浅灰
透明度: 50%
文字: 灰色
状态: 视觉弱化
```

**视觉示意**:
```
┌──────────────────────────┐
│   01  语义节点           │ ← 半透明
│      已确认              │
└──────────────────────────┘
```

**智能隐藏**: 当节点 > 5 时，已确认节点自动隐藏（除非是当前选中）。

---

### 4. 当前节点 (is-current)
```css
背景: 纯蓝色 (#0b5bd3)
文字: 白色
边框: 2px 深蓝
阴影: 0 4px 12px 蓝色光晕
```

**视觉示意**:
```
╔═══════════════════════════╗
║   02  数据收集    ★       ║ ← 白色文字
║      待审核               ║
╚═══════════════════════════╝
    ↑ 强阴影光晕
```

---

### 5. Hover 效果
```css
变换: 上浮 2px + 放大 2%
阴影: 0 4px 12px 蓝色
过渡: 180ms ease
```

**交互反馈**: 鼠标悬停时节点轻微放大并上浮，给予即时反馈。

---

## 📊 压缩统计卡片

当节点数 > 5 时显示：

```
┌────────────────────────────────────┐
│ 23已确认 | 7待处理 | 共 30 步      │
│  ↑绿色    ↑蓝色     ↑灰色         │
└────────────────────────────────────┘
```

**字段说明**:
- **已确认**: 绿色加粗，`creator.frozen_steps.length`
- **待处理**: 蓝色加粗，`reviewCount + unresolvedCount`
- **共 N 步**: 灰色，`totalCount`

---

## 🔢 节点数量场景

### 场景 1: 小项目（5 个节点）
```
[搜索框] [01 开始] [02 收集] [03 处理] [04 输出] [05 完成]
          待审核    待审核    待补齐    待审核    待审核
```
全部显示，无折叠。

---

### 场景 2: 中型项目（30 个节点）
```
[搜索框] [3已确认 | 8待处理 | 共30步] [03 能力缺口] [08 数据验证] [12 结果生成]
                                      待补齐 ⚠️    待审核        待审核
```
- 隐藏已确认节点
- 只显示待处理节点（优先级：待补齐 > 待审核）
- 最多显示 4 个

---

### 场景 3: 大型项目（100 个节点）
```
[搜索框] [67已确认 | 33待处理 | 共100步] [12 缺口A] [23 缺口B] [45 缺口C] [78 审核D]
                                         待补齐⚠️   待补齐⚠️   待补齐⚠️   待审核
```
- 如果有多个待补齐，优先显示它们
- 如果待补齐少于 4 个，补充待审核节点
- 用户可通过搜索框跳转到任意节点

---

## 🎬 动画设计

### 脉冲呼吸动画（待补齐节点专用）
```css
@keyframes pulse-attention {
  0%, 100% {
    box-shadow: 0 0 0 0 rgba(180,35,24,0.4);
  }
  50% {
    box-shadow: 0 0 0 8px rgba(180,35,24,0);
  }
}
```

**效果**: 红色阴影从节点边缘扩散到 8px 外，2秒循环，透明度从 40% 衰减到 0。

### Hover 动画
```css
transition: all 0.18s ease;
transform: translateY(-2px) scale(1.02);
```

**效果**: 180ms 内平滑上浮 2px 并放大 2%。

---

## 🧩 组件结构

```jsx
<nav className="semantic-node-route">
  {/* 搜索框 - 始终显示 */}
  <Field.Select className="semantic-node-search" />
  
  {/* 压缩统计 - 节点 > 5 时显示 */}
  {routeNodes.length > 5 && (
    <div className="semantic-route-compact">
      <strong>{confirmedCount}</strong>已确认
      <i /> {/* 分隔线 */}
      <em>{pendingCount}</em>待处理
      <i />
      <span>共 {totalCount} 步</span>
    </div>
  )}
  
  {/* 焦点节点区 */}
  <div className="semantic-route-focus">
    {focusNodes.map(node => (
      <Button className={statusClass}>
        <span className="semantic-node-index">01</span>
        <span className="semantic-node-label">节点名称</span>
        <span className="semantic-node-status">待补齐</span>
      </Button>
    ))}
  </div>
</nav>
```

---

## 🎯 优先级排序算法

```javascript
if (routeNodes.length <= 5) {
  // 显示全部节点
  return routeNodes
} else {
  // 智能过滤
  const focusNodes = routeNodes.filter(node => {
    const trusted = creator.frozen_steps.includes(node.id)
    const isUnresolved = node.resolution?.status === 'unresolved'
    const isSelected = node.id === selectedId
    
    // 保留条件：
    // 1. 未确认（待审核或待补齐）
    // 2. 是当前选中节点
    return !trusted || isUnresolved || isSelected
  }).slice(0, 4) // 最多 4 个
  
  return focusNodes
}
```

**排序逻辑**:
1. 待补齐节点优先
2. 待审核节点其次
3. 当前选中始终显示
4. 已确认自动隐藏

---

## 🔍 搜索框集成

### 功能
- 始终显示（不受节点数量限制）
- 可搜索、可清除
- 显示格式: `01 · 节点名称`

### 使用场景
- 快速跳转到任意节点（包括已确认节点）
- 100 个节点时的主要导航方式

---

## 🌈 颜色系统

| 状态 | 主色 | 边框 | 文字 | 背景渐变 |
|------|------|------|------|----------|
| **待补齐** | `#b42318` 红 | 2px 左侧 4px | 红色加粗 | `#fff7ed → #fff` |
| **待审核** | `#0b5bd3` 蓝 | 2px 左侧 4px | 蓝色加粗 | `#e8f1fd → #fff` |
| **已确认** | `#667085` 灰 | 1px 灰色 | 灰色 | `#f6f8fb` |
| **当前** | `#0b5bd3` 蓝 | 2px 深蓝 | 白色 | 纯蓝 `#0b5bd3` |

---

## 📐 尺寸规范

```css
/* 节点卡片 */
padding: 8px 12px;
border-radius: 6px;
font-size: 13px;

/* 序号 */
font-size: 11px;
font-weight: 700;

/* 标签 */
font-size: 13px;
font-weight: normal;

/* 状态 */
font-size: 10px;
font-weight: 500-700 (根据状态)

/* 角标 */
width/height: 18px;
font-size: 11px;

/* 统计卡片 */
padding: 4px 10px;
font-size: 11px;
```

---

## ✅ 优势对比

| 方面 | 旧设计（全部显示） | 新设计（智能焦点） |
|------|-------------------|-------------------|
| **5 节点** | ✅ 清晰 | ✅ 清晰 |
| **30 节点** | ❌ 横向滚动，信息过载 | ✅ 只显示 4 个重点 |
| **100 节点** | ❌ 完全不可用 | ✅ 焦点 + 搜索完美解决 |
| **紧急任务识别** | ❌ 需要逐个查看 | ✅ 脉冲动画自动吸引注意 |
| **已完成任务** | ❌ 占用空间 | ✅ 自动隐藏 |
| **视觉层次** | ❌ 所有节点平等 | ✅ 待补齐 > 待审核 > 已确认 |

---

## 🎯 用户心智模型

### 用户看到的
1. **一眼看到问题**: 红色闪烁 = 有卡带能力缺口
2. **知道工作量**: "7待处理" = 还需要审核 7 个节点
3. **清楚进度**: "23已确认" = 已经完成了大部分
4. **无需滚动**: 最多 4 个卡片，一屏完全可见

### 操作流程
1. 打开 Intent Studio
2. 看到红色脉冲节点 → 点击
3. 在详情面板中补齐能力
4. 节点变蓝色（待审核）
5. 审核完成 → 节点自动隐藏
6. 下一个红色/蓝色节点自动显示

---

## 🚀 扩展能力

### 支持的项目规模
- ✅ 小型 (3-10 节点): 全部显示
- ✅ 中型 (10-50 节点): 智能折叠
- ✅ 大型 (50-100 节点): 焦点 + 搜索
- ✅ 超大型 (100+ 节点): 同上，无瓶颈

### 性能
- 只渲染可见节点（最多 4 个）
- CSS 动画使用 GPU 加速
- 无虚拟滚动开销

---

## 🎨 设计灵感

1. **GitHub Actions** - 失败的步骤用红色高亮
2. **Jira 看板** - 只显示 "In Progress" 和 "Blocked"
3. **Notion 任务列表** - 已完成自动折叠
4. **Material Design** - 脉冲动画表示需要注意

---

## 📱 响应式考虑

当屏幕 < 1280px 时，整个导航器隐藏（已有逻辑），改用移动端布局：
- 显示当前节点卡片
- 提供下拉选择器切换
- 保持统计信息可见

---

**设计完成时间**: 2026-08-18  
**设计师**: Claude (Kiro)  
**原则**: Less is More, Focus on What Matters
