# CartridgeFlow TODO

原则：这里只放要干的事，不写工程仪式。出问题走 Git 时间线回滚，不再专门建立快照任务。

## 基本约定

- 改代码前，先确认这件事在 TODO 里。
- TODO 条目写短一点，能指导下一步就够。
- 一次只改一个明确问题，改完就提交。
- 协议旧版本不改；真要改协议，先讨论，再新建版本。
- 不提交依赖、缓存、日志、产物、密钥。

## 已完成

- [x] 重建 TODO，改成后续修改入口。
- [x] 重建 `AGENT.md`，提醒后续 Agent 先看 TODO。
- [x] 删除废弃脚本 `_gen_mega_flow.py`。

## 当前要干

- [x] 测试台运行列表：后端 `list_runs()` 必须按运行更新时间排序，不能按随机 run_id 目录名排序。
- [x] 测试台交互：AI 决策提交后立即锁定当前交互，隐藏气泡并禁止再次打开同一决策窗。
- [x] 素材工坊：`asset_specs.policy` 即使被 AI 输出成字符串也不能崩溃，要规范化为策略对象。
- [x] 测试台交互：AI 确认类节点的“确认决策”要一键提交，避免确认时还要再点一次提交。
- [x] LLM 客户端：真实 AI 调用结束后要显式关闭异步 HTTP 客户端，避免 CMD 里刷 `Event loop is closed`。
- [x] 测试台详情：节点完成或失败后要清掉旧的待交互信息，避免已通过节点看起来还要交互。
- [x] 测试台日志：流程暂停等待用户时不要记录为 `run_completed`，要明确记录为暂停。
- [x] 测试台运行：协作节点暂停恢复后，不能丢失同一分叉下未执行的兄弟分支，不能中途假完成。
- [x] v3 卡带：素材复核分支要正确回到创作素材包汇合节点，保证素材结果被后续消费。
- [x] 测试台详情：对象和 JSON 数据要格式化展示，方便阅读。
- [x] 简化 `TODO.md` 和 `AGENT.md`，去掉过度流程化内容。
- [x] 测试台 UI：探针范围选中后不要显示下拉框。
- [x] 测试台 UI：运行按钮和刷新按钮放进“运行范围”容器。
- [x] 测试台 UI：决策模式只保留“真实协作”和一个 Mock。
- [x] 测试台 UI：等待用户交互时不要在左栏显示表单；节点闪红，画布提示气泡可打开交互界面。
- [x] 测试台 UI：节点详情改成可折叠；默认展开第一个；展开内容尽量占满空间；支持弹窗展开查看。
- [x] 测试台 UI：修诊断信息和日志文字显示，避免重叠、竖排、挤压。
- [x] 测试台 UI：日志栏最大高度按视口和缩放动态调整，最多到运行范围下方附近。
- [x] 测试台协作：提交用户交互后按钮不要一直卡住；恢复时状态要及时释放。
- [x] 测试台协作：AI 确认类交互要有固定按钮，例如“确认决策 / 驳回决策”。
- [x] 测试台诊断：查最近一轮真实协作报错和“可选输入缺失”的原因，只显示真问题。
- [x] 测试台交付：运行完成后要有本次交付产物入口，支持打开、复制路径和按类型识别。
- [x] 测试台交付：交付产物入口不要放左侧栏，改成画布按钮展开，和“查看 UI”一致。
- [x] 新建 `dev.spatial_blockout_lab`：轻量 3D 草图卡带，AI 生成规格，工具产出 GLB/HTML 预览。
- [x] 3D 草图卡带：运行前阻止必填入料为空，避免 04 AI 决策节点反复等待补料。
- [x] 3D 草图卡带：从静态 blockout 改成动画预演，产出角色代理、动作关键帧、镜头时间轴和可播放 HTML 预览。
- [ ] 暂停继续强化 AI 素材生成；第一阶段只使用现成角色、现成场景、现成道具和现成动作库。
- [ ] 新建 `dev.series_3d_episode_factory`：3D 连续短剧工厂卡带，目标是 10-20 秒竖屏短剧预演。
- [ ] 短剧工厂：定义固定资产库索引，包括角色、场景、道具、动作标签、镜头模板，不让 LLM 直接生成美术资产。
- [ ] 短剧工厂：AI 编剧节点输出剧情钩子、单集脚本、结尾悬念，必须是结构化 `episode_script`。
- [ ] 短剧工厂：AI 分镜节点输出 3-5 个镜头的 `shot_list`，包含画面目标、角色、对白、节奏、镜头模板。
- [ ] 短剧工厂：资产匹配节点把 `shot_list` 映射到现成资产库，缺资产就报明确缺口，不自动生成新资产。
- [ ] 短剧工厂：动作匹配节点只输出动作标签和时长，例如 `walk_slow`、`turn_head`、`idle_talk`，不让 LLM 生成复杂动作。
- [ ] 短剧工厂：Blender 编排节点消费脚本、分镜、资产、动作、镜头模板，生成可预览工程或视频。
- [ ] 短剧工厂：用户复核不满意时回到脚本或分镜节点局部重做，不回滚资产库。
- [ ] 测试台：前端显示 AI 决策的“生成了什么、后续消费了什么”，方便确认 LLM 只做擅长的结构化决策。

## 近期顺序

1. 建 `dev.series_3d_episode_factory` 主线卡带。
2. 建最小现成资产库索引。
3. 打通脚本、分镜、资产匹配、动作匹配、Blender 编排的最短链路。
4. 再补测试台的决策消费可视化。
