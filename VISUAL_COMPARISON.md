# 节点导航器视觉对比

## 改进前的设计

```
┌────────────────────────────────────────────────────────────────┐
│  ○  ───  ○  ───  ○  ───  ○  ───  ○                            │
│  01      02      03      04      05                            │
│  ·       ·       ·       ·       ·    (上标符号，很小)        │
└────────────────────────────────────────────────────────────────┘

特点：
- 圆形按钮（30px → hover 时 38px）
- 颜色圆点表示状态（橙色/绿色）
- 连接线分隔
- 信息密度低，只有序号
```

## 改进后的设计

```
┌────────────────────────────────────────────────────────────────┐
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──│
│  │01 开始   │→ │02 收集   │→ │03 处理   │→ │04 输出   │→ │05│
│  │   可信   │  │   待审核 │  │   待审核 │  │   待审核 │  │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  └──│
│     当前                                                       │
│  (白字蓝底)    (灰色边框)    (灰色边框)    (灰色边框)         │
│   + 阴影                                                       │
└────────────────────────────────────────────────────────────────┘
                                                        ▼ 渐变提示
```

---

## 详细状态示意

### 1. 当前节点 (is-current)
```
┌──────────────────┐
│ 01  语义节点     │  ← 白色文字
│     可信         │  ← 蓝色背景 (#0b5bd3)
└──────────────────┘  ← 深蓝边框 + 6px 阴影
     ↑ 加粗字体
```

### 2. 已确认节点 (is-trusted)
```
┌──────────────────┐
│ 02  数据收集     │  ← 深色文字
│     可信         │  ← 白色背景
└──────────────────┘  ← 绿色调边框
     ↑ 绿色状态文字
```

### 3. 待补齐节点 (is-unresolved)
```
┌──────────────────┐
│ 03  能力缺口     │  ← 深色文字
│     待补齐       │  ← 白色背景
└──────────────────┘  ← 红色调边框
     ↑ 红色状态文字
```

### 4. 待审核节点 (默认)
```
┌──────────────────┐
│ 04  结果呈现     │  ← 灰色文字
│     待审核       │  ← 白色背景
└──────────────────┘  ← 灰色边框
     ↑ 灰色状态文字
```

### 5. Hover 状态
```
┌──────────────────┐
│ 05  最终交付     │  ↑ 轻微上浮 (-1px)
│     待审核       │  ← 浅蓝背景
└──────────────────┘  ← 蓝色边框 + 4px 阴影
     ↑ 深蓝文字
```

---

## 流程方向指示

```
   →  02  收集    →  03  处理    →  04  输出
   ↑              ↑              ↑
   箭头           箭头           箭头
   (30%透明)      (30%透明)      (30%透明)
```

第一个节点不显示箭头，从第二个开始每个节点前都有 `→`。

---

## 滚动指示器

当节点数量超过可视区域时：

```
┌────────────────────────────────────────────────┐
│  01 → 02 → 03 → 04 → 05 → 06 → 07 → 08 → 09  ░│
│                                              ░░│
└──────────────────────────────────────────────░░┘
                                               ↑
                                        40px 渐变遮罩
                                    (透明 → 白色)
```

---

## 响应式适配

### 桌面端 (> 1440px)
- 节点宽度: **148px**
- 序号: **11px**
- 标签: **12px**
- 状态: **10px**

### 中等屏幕 (1280-1440px)
- 节点宽度: **120px** ⬇
- 序号: **11px**
- 标签: **11px** ⬇
- 状态: **9px** ⬇

### 小屏幕 (< 1280px)
- 导航器隐藏，改用下拉选择器

---

## 颜色系统

```
主题蓝:   #0b5bd3  ███  当前节点背景
深蓝:     #123c70  ███  当前节点边框
浅蓝:     #e8f1fd  ███  Hover 背景

成功绿:   #2f8f66  ███  可信状态
错误红:   #b42318  ███  待补齐状态
中性灰:   #667085  ███  待审核文字

边框灰:   #d5dde7  ───  默认边框
背景白:   #ffffff  ▯   节点背景
```

---

## 阴影层次

```
当前节点:  0 2px 6px rgba(11,91,211,0.25)  强阴影
Hover:     0 2px 4px rgba(0,0,0,0.08)      中阴影
默认:      无阴影                          平面
```

---

## 交互动画时间轴

```
Hover 进入:
  0ms ────────────────────── 150ms
  ·   背景色渐变
  ·   边框颜色渐变
  ·   transform: translateY(-1px)
  ·   阴影淡入
      
Hover 离开:
  0ms ────────────────────── 150ms
  ·   所有属性平滑恢复
```

过渡曲线: `ease` (缓入缓出)

---

## 信息层级

```
  01          ← 序号 (11px, 700 weight, 主题蓝)
  ──────
  语义节点    ← 标签 (12px, 正常, 自适应颜色)
  
  可信        ← 状态 (10px, 500 weight, 语义颜色)
```

垂直布局，左对齐，信息密度适中。

---

## 无障碍特性

- `aria-label`: "01 语义节点，可信"
- `title`: "01 · 语义节点 · 可信"
- `cursor: pointer` 明确可点击
- 键盘焦点环 (浏览器默认)
- 颜色对比度符合 WCAG AA 标准

---

## 浏览器兼容性

- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+

关键特性:
- `color-mix()` 需要现代浏览器
- `::before`/`::after` 广泛支持
- `transform` 和 `box-shadow` 硬件加速
- `transition` 全面支持

---

## 性能考虑

- 使用 `transform` 而非 `margin` (GPU 加速)
- 固定宽度避免回流
- 渐变遮罩用 `pointer-events: none` 避免事件捕获
- 过渡动画时长 150ms (人眼舒适区间)

---

## 最佳实践验证

✅ 状态一致性：同样的状态在任何位置显示相同  
✅ 视觉层次：序号 > 标签 > 状态  
✅ 反馈及时：Hover 在 150ms 内响应  
✅ 边界清晰：每个节点有明确的点击区域  
✅ 信息完整：序号、名称、状态一目了然  
✅ 方向明确：箭头指示流程顺序  
✅ 扩展友好：支持任意数量节点  
✅ 响应灵敏：适配不同屏幕尺寸  

---

**对比结论**：新设计在信息密度、视觉层次、交互反馈和可用性方面全面超越旧设计。
