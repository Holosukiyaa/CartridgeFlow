# CartridgeFlow TODO

> 这里只保留当前真正影响交付的开放任务。每项任务必须形成可独立验收的完整能力；完成历史由 Git 和 GitHub 保存。

## P0 · 当前阻断

当前没有已确认的 P0 阻断。发现会导致数据损坏、安全问题或主流程完全不可用的问题时，再在这里加入任务。

## P1 · 当前主线

- [ ] `CORE-001` 完成 `BASE@0.2` 与 `FARP@0.6` 的“协议条款 -> 实现 -> 自动测试”证据链；每项声明能力必须可验证，或明确保持部分支持并说明缺口。
- [ ] `RESOURCE-001` 为角色绑定补齐底座中立的 MCP、远程 HTTP 和 CLI 实际适配器；URL、密钥和本机路径只保存在本地配置，并覆盖真实成功与失败路径。
- [ ] `PKG-001` 打通卡带包的完整原子生命周期：清单与校验、依赖和权限预检、安装、升级失败回滚、停用、卸载及用户产物保留。
- [ ] `UI-001` 建立版本化的 DLC UI Host：统一请求、响应、错误、取消、超时、能力协商和大文件通道，并用权限沙箱隔离两张结构不同的卡带界面。
- [ ] `ART-001` 完成正式产物闭环：来源、revision、审批、下游失效、主交付物校验、导出和脱敏诊断包使用同一套可追溯记录。

## P2 · 生产验收

- [ ] `TEST-001` 建立覆盖 Provider、工具、Worker、进程重启和非幂等副作用的故障注入矩阵；用户不查看服务端日志也能选择合法的恢复动作。
- [ ] `REF-001` 在底座之外制作一张真实参考卡带，按公开支持矩阵走通安装、预检、运行、人工修改、恢复、交付、升级和卸载。
- [ ] `RELEASE-001` 建立统一发布门槛：版本一致性、依赖与运行环境摘要、协议能力报告、敏感数据扫描和可复现的测试报告必须随候选版本生成。
- [ ] `DIST-001` 在生命周期和参考卡带验收通过后，提供正式 schema、SDK 与 CLI，并补齐包签名、完整性校验、兼容性判断和发布者信任链。

## 维护规则

- 开放任务总数不超过 12 项；超过时先合并、排序或移除，而不是继续堆积。
- 只记录跨模块完整能力、发布阻断或高风险可靠性问题。
- 局部样式、文件整理、命名调整、普通重构和单条测试不单独进入 TODO。
- 每项任务必须说明结果和验收边界，不能只写“优化”“完善”或实现步骤。
- 任务完成后直接移出本文件；提交记录、自动报告和 GitHub 负责保存历史。
