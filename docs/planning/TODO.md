# CartridgeFlow TODO

> 本轮前端可用性、沙箱交互、工具模型收敛和卡带迁移报告已经完成。当前没有开放任务，下一轮工作由项目负责人重新评估和排序。

## 当前状态

v0.2.0 与 v0.3.0 已发布完成。

## 已冻结队列

原有 `CORE-001`、`PKG-001`、`UI-001`、`ART-001`、`TEST-001`、`REF-001`、`RELEASE-001` 和 `DIST-001` 暂停执行。任务内容由 Git 历史保存，重新评估前不作为当前待处理项展示。

## 维护规则

- 开放任务总数不超过 12 项；超过时先合并、排序或移除，而不是继续堆积。
- 只记录跨模块完整能力、发布阻断或高风险可靠性问题。
- 局部样式、文件整理、命名调整、普通重构和单条测试不单独进入 TODO。
- 每项任务必须说明结果和验收边界，不能只写“优化”“完善”或实现步骤。
- 任务完成后直接移出本文件；提交记录、自动报告和 GitHub 负责保存历史。

## 当前任务：梳理工作台交互配置与节点详情

> 当前行动：本轮交互节点配置台、节点右侧栏与顶部操作层级已完成并通过构建、截图与格式验证。

- [x] `UI-400` 统一工作台顶部按钮命名与设计/测试模式顺序。
- [x] `UX-401` 将交互节点页改为组件优先的可视化配置台：组件列表与表单为主，素材作为组件依赖，原始 JSON 收进高级模式。
- [x] `UX-402` 将节点右侧栏分为核心配置、节点专项、连接摘要和高级协议四层，消除重复字段并默认隐藏协议实现细节。
- [x] `UI-403` 完成顶部按钮修改的截图与生产构建验证。

## 当前任务：验证中转站生图 API 接入方式

> 当前行动：方向已取消；底座不再维护逆向图片执行器，媒体能力改由工具连接承接官方 API 或自部署服务。

- [x] `INT-410` 确认中转站提供的认证、请求协议、图片输出形式及模型名称。
- [x] `INT-411` 判断现有本地模型配置能否直接连接，给出不保存密钥的安全配置步骤。
- [x] `INT-412` 明确当前生图 Flow 还缺少的执行器或适配能力。
- [x] `IMG-413` 获取并固化 Hubway 逆向生图请求契约，或实现标准 Images API 图片执行器。
- [x] `IMG-414` 已取消：不再为逆向图片执行器增加连通性、产物与失败诊断。

## 当前任务：落地 Img2 模型 API 适配器

> 当前行动：Img2 Provider 字段、资源中心选择、能力匹配、文本测试边界与缩放验证已完成；真实媒体执行仍等待逆向接口契约。

- [x] `IMG2-401` 增加模型能力与适配器字段，保持旧配置兼容。
- [x] `IMG2-402` 在模型 API 配置界面加入图片能力与 Img2 适配器选择。
- [x] `IMG2-403` 增加 Img2 配置校验和清晰的文本/媒体测试边界。
- [x] `IMG2-404` 通过构建、接口回归和缩放截图验证。

## 当前任务：修复模型 API 新增表单回归

> 当前行动：输入框失焦和新增连接回归已修复，构建、接口冒烟与 Python 回归均已通过。

## 当前任务：修复资源配置权限错误

> 当前行动：已区分模型服务鉴权失败与底座本地权限错误，完成代码级回归；重启后端即可生效。

- [x] `IMG2-408` 定位权限错误的前后端来源。
- [x] `IMG2-409` 修复模型连接新增/保存时的权限误判或请求链路。
- [x] `IMG2-410` 完成前端构建与权限错误回归验证。

## 当前任务：真实仿真 Img2 中转站

> 当前行动：yyapi Images API、真实生图测试、Base64/URL 解析、错误诊断和前端配置已完成；Flow 产物落盘仍保留在 `IMG-414`。

- [x] `IMG2-411` 使用已保存 Key 探测 `/models`、`/responses` 与兼容路径。
- [x] `IMG2-412` 完成最小文本请求，确认模型名和 Responses 协议是否可用。
- [x] `IMG2-413` 完成最小图片请求，记录请求体、返回结构和失败原因。
- [x] `IMG2-414` 根据真实仿真结果修正适配方案，未跑通前不得宣称可用。
- [x] `IMG2-415` 阅读新中转站飞书文档，提取 Base URL、认证、模型、端点、请求体与返回体。
- [x] `IMG2-416` 按新文档建立最小仿真请求，并在不泄露 Key 的前提下验证真实图片响应。
- [x] `IMG2-417` 增加 OpenAI Images API 传输类型与图片生成适配器。
- [x] `IMG2-418` 将模型配置界面的 Img2 改为 Images API，并提供真实生图测试。
- [x] `IMG2-419` 验证 Base64/URL 解析、错误分类、构建与缩放布局。

## 当前任务：修复配置中心删除连接

> 当前行动：模型连接删除、子页面即时移除、外层资源中心同步刷新和 125% 布局验证均已完成。

- [x] `RES-420` 验证模型连接 DELETE 接口与错误反馈。
- [x] `RES-421` 修复资源配置弹窗删除后外层资源中心不刷新的问题。
- [x] `RES-422` 完成临时连接端到端删除与前端构建验证。

## 当前任务：修复真实生图按钮 500

> 当前行动：确认本次为上游瞬时失败；已修复模型 429/5xx/超时分类与前端详细错误显示，真实生图复测成功。

- [x] `IMG2-420` 定位测试生图按钮触发的后端 500 根因。
- [x] `IMG2-421` 修复图片测试错误封装或执行异常，保留可诊断信息。
- [x] `IMG2-422` 通过真实按钮等价请求、自动测试和前端反馈回归。

## 当前任务：仿真 Codex 图片扩展模式

> 当前行动：mggoo Codex 图片扩展已完成真实端到端验证；31 个 LLM 测试、前端构建、100%/125% 视觉检查均通过，连接已标记“生图可用”。

- [x] `IMGX-430` 探测模型列表路径与特殊请求头鉴权（两个模型列表路径均返回 200）。
- [x] `IMGX-431` 完成最小 Responses 文本请求并记录输出结构（标准 `response` / `message` / `completed`）。
- [x] `IMGX-432` 完成图片扩展请求：Responses 直接返回已完成的 `image_generation_call.result`，不是二次 `/images/generations` 调用。
- [x] `IMGX-433` 根据真实契约决定新增独立的 Codex 图片扩展适配器：`Responses + image_generation_call`，与 `/images/generations` 分离。
- [x] `IMGX-434` 梳理现有连接类型、Provider 工厂和测试接口，确定最小改动边界。
- [x] `IMGX-435` 实现 Codex 图片扩展请求与返回解析，支持静态执行器请求头且不暴露密钥。
- [x] `IMGX-436` 在模型配置页加入明确的连接类型和真实“测试生图”入口。
- [x] `IMGX-437` 补齐后端自动测试、前端构建，并用真实 mggoo 连接完成端到端验证。

## 当前任务：卡带管理空状态与真实生图卡带

> 当前行动：卡带管理空状态已完成；原逆向生图卡带计划已随媒体模型适配器一并取消。

- [x] `CART-440` 重做卡带管理零数据状态，提供清晰入口且适配 100%/125% 缩放。
- [x] `CART-441` 已取消：不创建依赖本机逆向 `image_generation` 模型连接的生图卡带。
- [x] `CART-442` 已取消：不为已撤下的逆向图片执行链补充 Base64 产物逻辑。
- [x] `CART-443` 已取消：不再绑定或验证 mggoo Codex 图片扩展。
- [x] `CART-444` 已取消：对应逆向生图卡带的专项验证不再执行。

## 当前任务：Hubway Codex 图片连接测试无反馈

> 当前行动：修复完成；Hubway 真实失败反馈、错误身份、自动测试、前端构建及 100%/125% 弹窗布局均已验证。

- [x] `CONN-445` 复现 hubway Codex 图片连接测试并取得真实 HTTP 结果。
- [x] `CONN-446` 修复测试失败无提示或错误被吞掉的问题。
- [x] `CONN-447` 增加回归测试并验证资源中心反馈状态。

## 当前任务：mhapi Codex 图片扩展真实验证

> 当前行动：验证完成；新分组通过 `gpt-image-2 + Images API` 生成了清晰有效的 1024x1024 PNG，正式连接应使用 Img2 图片生成适配器。

- [x] `MHAPI-448` 确认 `Responses + local-image-extension` 请求头及临时测试方式。
- [x] `MHAPI-449` 对 `https://api.mhapi.cn` / `gpt-5.5` 执行真实生图测试。
- [x] `MHAPI-450` 记录可用性、响应格式和后续连接配置建议。
- [x] `MHAPI-451` 确认切换分组后的模型列表与路由状态。
- [x] `MHAPI-452` 使用新分组完成一次真实生图回归并记录结果。

## 当前任务：模型连接自动检测与填写

> 当前行动：自动检测与 OpenCode 一键导入完成；37 项测试、前端构建、真实 mhapi 回归、100%/125% 截图和密钥泄漏检查均通过。

- [x] `AUTODETECT-453` 定义本地检测接口及统一的能力、协议、模型与 URL 推断结果。
- [x] `AUTODETECT-454` 实现模型列表探测、适配器识别和 URL 自动规范化。
- [x] `AUTODETECT-455` 在新增/编辑连接弹窗加入“检测并自动填写”，隐藏普通用户不必处理的协议细节。
- [x] `AUTODETECT-456` 补齐成功、鉴权失败、无模型和现有密钥复用的自动测试。
- [x] `AUTODETECT-457` 执行前端构建、后端回归和 100%/125% 视觉检查。
- [x] `AUTODETECT-458` 将一键导入统一为 OpenCode JSON，并支持 options.baseURL、apiKey 与 models。
- [x] `AUTODETECT-459` 导入后自动检测并回填适配器、协议、模型和规范化 URL。

## 当前任务：移除 Codex 图片扩展

> 当前行动：移除完成；33 项 LLM 测试、前端构建、后端重启和资源中心截图均通过，当前仅保留普通文本与 Img2 连接。

- [x] `REMOVE-CODEX-460` 移除 Codex 图片适配器与运行调用路径。
- [x] `REMOVE-CODEX-461` 移除自动检测、导入器和前端配置选项中的 Codex 图片语义。
- [x] `REMOVE-CODEX-462` 清理现有本机 Codex 图片连接，保留 Img2 与普通文本连接。
- [x] `REMOVE-CODEX-463` 更新测试并执行构建、回归与界面检查。

## 当前任务：检测模型选择与卡带覆盖

> 当前行动：模型选择完成；17 项相关测试、前端构建及真实 11 模型连接的 100%/125% 布局检查通过。

- [x] `MODEL-CHOICE-464` 在连接主配置区直接展示并切换检测到的默认模型。
- [x] `MODEL-CHOICE-465` 卡带绑定模型连接时允许从该连接候选模型中选择实际模型。
- [x] `MODEL-CHOICE-466` 保证模型覆盖写入本机 assignments，不修改卡带配方。
- [x] `MODEL-CHOICE-467` 补齐测试、构建与 100%/125% 界面检查。

## 当前任务：手动声明连接用途与统一新增入口

> 当前行动：手动连接用途、统一新增入口、真实检测与缩放布局均已完成验证。

- [x] `CONNECTION-KIND-468` 为 Provider 与检测接口加入显式 connection_type，停止根据模型名称猜用途。
- [x] `CONNECTION-KIND-469` 新增连接第一步选择普通 LLM、图片生成或视频生成。
- [x] `CONNECTION-KIND-470` 将 OpenCode 导入移动到“新增”统一入口，移除模型区独立导入按钮。
- [x] `CONNECTION-KIND-471` 视频连接可登记但在执行适配器完成前明确阻止绑定与运行。
- [x] `CONNECTION-KIND-472` 更新测试并完成真实检测、构建和 100%/125% 截图检查。

## 当前任务：收敛模型连接并重塑全局界面

> 当前行动：模型连接已收敛为普通 LLM，一级页面、缩放布局与 Lucide 图标系统均已完成验证。

- [x] `UI-RESET-473` 从模型配置、检测、存储和运行链路移除图片/视频连接类型及专用适配器。
- [x] `UI-RESET-474` 保留 OpenCode 一键导入，并让新增连接回归普通 LLM 语义。
- [x] `UI-RESET-475` 在工具/远程 API 能力中明确承接官方生图、生视频 API 与用户自部署服务。
- [x] `UI-RESET-476` 对比 img2-ui 四张不同参考图，提炼共同优点并确定唯一的颜色、字体、间距、容器、按钮与导航规范。
- [x] `UI-RESET-477` 按唯一视觉基线统一改造所有一级页面及共享外壳，避免不同页面各自模仿成四套风格，并保持 100%/125% 缩放可用。
- [x] `UI-RESET-478` 完成后端测试、前端构建、页面截图与关键交互回归。
- [x] `UI-RESET-479` 选用成熟的开源线性图标库，替换侧栏字符符号并建立统一图标尺寸、描边和状态规则。

## 当前任务：125% 缩放溢出专项修复

> 当前行动：纵向收敛已完成；100%/110%/125% 下一级页面的横向与纵向滚动均为 0，长数据只在指定的列表或正文容器内部滚动。

- [x] `ZOOM-480` 审计概览、卡带管理、运行诊断、资源中心、打包发布、其他设置及资源配置弹窗的 125% 溢出。
- [x] `ZOOM-481` 修复页面网格、标题操作区、卡片和弹窗的横向或纵向越界。
  - 已确认概览统计区仍在四等分，带 7 日图表的卡片宽度不足；资源配置弹窗在低高度视口需要更明确的上下安全边距。
  - 已按内容权重重排中宽统计栏，并为低高度资源配置弹窗补上视口安全高度和受控内部滚动。
- [x] `ZOOM-482` 完成 100%/125% 截图、DOM 溢出检查、前端构建和关键交互回归。
- [x] `ZOOM-483` 检测所有一级页面的纵向 `scrollHeight/clientHeight`，消除页面级纵向滚动。
  - 已确认仅全局概览产生页面级纵向滚动：125% 下主区域为 `720/984`；其余一级页面均为 `720/720`。
  - 根因是统一主题覆盖了概览原有的低高度紧凑规则，标题、统计区和卡片间距累计超出一屏。
- [x] `ZOOM-484` 复核内部滚动容器、100%/110%/125% 截图和生产构建，确保页面双轴滚动均为 0。

## 当前任务：统一现代明亮视觉语言

> 当前行动：现代明亮视觉语言已统一完成；所有一级页面和资源弹窗已通过三档缩放截图、双轴溢出检测与生产构建。

- [x] `STYLE-490` 审计现有主题中残留的复古字体、米黄色、大面积灰色、厚边界和重复卡片层级。
- [x] `STYLE-491` 建立中性白、深石墨、微暖橙色的统一颜色与表面系统，并限制功能色用途。
- [x] `STYLE-492` 统一概览、卡带管理、运行诊断、资源中心、打包发布、其他设置和资源弹窗的字体、8px 间距、6–10px 圆角与微量阴影。
  - 已将普通 UI 字体收敛为现代无衬线，等宽字体仅保留给代码、ID、路径和时间等技术字段。
  - 已将一级模块收敛为 8px 圆角、1px 中性边界和微量双层阴影；内部层级优先使用分隔线与浅背景。
  - 截图复核发现“开发者”字体选项仍描述为全局等宽；将改为“现代精密”，同时保留系统默认和经典字体的真实切换能力。
- [x] `STYLE-493` 完成 100%/110%/125% 截图、双轴溢出检测、生产构建和关键交互回归。

## 当前任务：Img2 驱动的一级页面布局重构

> 当前行动：正在按 Img2 稿件的实际纵向比例纠偏：页头固定在约 90px，主体从高位开始并铺满剩余高度；卡带页补回分组行并禁止单卡拉伸，概览重新分配首行、中段和底栏高度。

- [x] `REDESIGN-500` 梳理一级页面现有功能、必须保留的信息和交互，形成不能被视觉稿删除的功能清单。
- [x] `REDESIGN-501` 编写 Img2 统一母提示词，明确品牌、受众、视觉语言、布局约束、组件规则与禁止项。
- [x] `REDESIGN-502` 为全局概览、卡带管理、运行诊断、资源中心、打包发布、其他设置分别编写页面提示词和状态变体。
- [x] `REDESIGN-503` 生成并评审同一视觉体系下的页面稿，未通过前不迁移前端功能。
- [x] `REDESIGN-504` 建立与旧页面隔离的新前端路由和共享视觉 token，不改后端、不删除旧页面。
- [x] `REDESIGN-505` 第一层只实现新页面外壳与骨架布局，使用稳定占位数据验证结构和缩放。
- [x] `REDESIGN-506` 第二层加入静态展示组件和完整状态，逐页与 Img2 稿件对照。
- [x] `REDESIGN-507` 第三层复用现有 API 接入真实只读数据，再逐项连接写操作、弹窗和导航。
- [x] `REDESIGN-508` 完成功能对照、100%/110%/125% 截图、双轴零页面滚动和新旧页面切换验收。

- [x] `IMG2-405` 修复模型 API 表单输入时焦点丢失。
- [x] `IMG2-406` 修复新增模型连接保存失败。
- [x] `IMG2-407` 完成前端构建与新增流程回归验证。

## 当前任务：按评分重做 Img2 概览页

> 当前行动：页面验收已通过；5174 静态服务器已改为静默请求处理并重新启动，跨命令检查确认 Python 进程持续监听、首页返回 200 且加载当前 pages-2 资源。

## 当前任务：统一新前端图标、素材与动效系统

> 当前行动：已完成资产迁移、动效接入与双档视口验收；仅保留 CartridgeFlow 品牌 Logo，功能图标、技术标识和交互动效均由锁定的本地依赖承载。

- [x] `ASSET-520` 建立新前端资产审计清单，区分品牌资产、功能图标、品牌图标、插画、状态图形和图表。
- [x] `ASSET-521` 使用 Lucide 替换全部功能性自绘 SVG、字符图标和临时符号，统一尺寸、线宽、对齐与无障碍标签。
- [x] `ASSET-522` 使用 Simple Icons 补齐 Python、Node.js、Git、FFmpeg 等品牌/技术标识，并将所需资源本地打包。
- [x] `MOTION-523` 建立页面进入、弹窗、列表选择/删除、展开折叠、Toast 和设置预览的统一动效规范，支持 `prefers-reduced-motion`。
- [x] `MOTION-524` 完成所有独立页面的图标、动效、交互、100%/125% 零溢出和服务器可访问性回归。

## 当前任务：精修图标与控件细节

> 当前行动：成品组件选型和 Switch 试点已通过；采用 Web Awesome 按需承接复杂通用控件，页面骨架和业务展示继续保持项目自有设计。

- [x] `POLISH-525` 重做打包发布步骤条的节点、连接线和当前步骤状态，移除孤立箭头的临时感。
- [x] `POLISH-526` 审计概览、卡带管理、运行诊断、资源中心、打包发布和其他设置中的图标对齐、按钮留白与状态语义。
- [ ] `POLISH-527` 修正发现的视觉细节，并保持 Lucide、Simple Icons 和现有布局边界不变。
- [ ] `POLISH-528` 完成交互、100%/125% 截图与双轴零溢出回归。
- [x] `COMP-529` 调研可在原生 HTML 中按需使用的成品组件库，比较视觉可定制性、包体、无障碍、维护状态和框架侵入性。
- [x] `COMP-530` 选择一个最小试点组件，在不改后端和页面骨架的前提下验证主题覆盖与 100%/125% 布局。

## 当前任务：修复新组件改动不可见

> 当前行动：已确认用户浏览器同时打开 5173 旧 React 前端和 5174 独立新前端；新版资源正常，现已将 5174 切到打包发布并置于前台。

- [x] `VISIBLE-531` 确认 5174 监听进程和静态目录是否指向 `src/frontend-new`。
- [x] `VISIBLE-532` 确认浏览器实际加载 `components-1` 资源，并检测 `.release-connector` 与 `wa-switch`。
- [x] `VISIBLE-533` 修复缓存、服务进程或视觉差异不明显的问题，并完成用户可见截图回归。

## 当前任务：概览页卡片质感

> 当前行动：概览页一级容器阴影已加深一档，100%/125% 截图与双轴零溢出检查通过。

- [x] `OVERVIEW-534` 确认概览页一级容器范围，避免给内部小组件重复加阴影。
- [x] `OVERVIEW-535` 增加克制的卡片阴影和边框明度调整。
- [x] `OVERVIEW-536` 完成 100%/125% 截图与双轴零溢出检查。
- [x] `OVERVIEW-537` 加深近层与远层阴影并重新完成双档截图检查。

## 当前任务：概览页空间合理化

> 当前行动：保留现有模块和视觉语言，将概览主体从两条强制等高横排调整为左右独立内容列；压缩最近工作，把弹性空间交给 TODO 列表。

- [x] `OVERVIEW-538` 明确最近工作、运行活动、TODO、能力、诊断和资源接线的空间职责与最小高度。
- [x] `OVERVIEW-539` 在不重写模块内容的前提下改造概览网格和最近工作内部间距。
- [x] `OVERVIEW-540` 检查空 TODO、多条 TODO、长卡带名称以及 100%/125% 的布局稳定性。
- [x] `OVERVIEW-541` 完成截图、双轴零溢出和关键交互回归。

## 当前任务：独立前端接入正式功能

> 当前行动：以现有后端 API 为唯一事实来源，移除侧栏中的稿件占位入口，先完成一级页面的真实读取、刷新、导航和关键操作，再执行 100%/125% 布局与交互回归。

- [x] `LIVE-542` 阅读 AI 快速起点、独立前端边界、旧前端 API 封装和后端路由，建立页面到真实能力的映射。
- [x] `LIVE-543` 将侧栏收敛为底座真实拥有的一级菜单，并统一中文名称、选中状态和页面路由。
- [x] `LIVE-544` 接入全局概览的真实运行统计、最近 Flow、TODO、能力证据与本地资源状态。
- [x] `LIVE-545` 接入卡带管理的真实列表、空状态、刷新、打开目录、删除和进入工作台行为。
- [x] `LIVE-546` 接入运行诊断的真实运行列表、筛选、详情、删除、复制诊断和导出行为。
- [x] `LIVE-547` 接入资源中心、打包发布和系统设置当前可由底座提供的真实读取与操作能力。
- [x] `LIVE-548` 完成 API 失败态、加载态、空状态、100%/125% 双轴零溢出和关键交互回归。

## 当前任务：概览页可读性与空间利用

> 当前行动：仅调整独立前端概览页，将卡片内的无效留白转化为更大的标题、正文、图标、数字和操作热区；保持真实数据接线与页面模块不变。

- [x] `OVERVIEW-550` 审计概览页 100%/125% 下的小字、小图标、过小点击区和无效留白。
- [x] `OVERVIEW-551` 放大页头统计、最近工作、运行活动、TODO、能力、诊断和资源接线的核心信息层级。
- [x] `OVERVIEW-552` 使用容器响应规则保证 125% 下仍完整可见，禁止以 8px/9px 字体解决空间问题。
- [x] `OVERVIEW-553` 完成真实数据截图、双轴零溢出和概览关键入口回归。

## 当前任务：概览页容器级视觉精修

> 当前行动：六个概览容器已完成 100%/125% 独立截图、几何审计与真实数据回归；页面双轴溢出为 0，按钮热区、内容密度和容器内边距已统一收敛。

- [x] `OVERVIEW-554` 为最近工作、运行活动、TODO、底座能力、运行诊断和本地资源生成 100%/125% 独立截图与尺寸报告。
- [x] `OVERVIEW-555` 修复容器内部裁剪、非预期溢出、过小字体和按钮热区。
- [x] `OVERVIEW-556` 统一六个容器的边缘留白、分组间距、分隔线和纵横对齐关系。
- [x] `OVERVIEW-557` 收敛过大空位，让列表、图表、摘要和资源状态按内容权重填充可用空间。
- [x] `OVERVIEW-558` 完成容器截图复核、真实数据回归与 100%/125% 双轴零溢出验收。

## 当前任务：固化容器空间精修 Skill

> 当前行动：`polish-ui-layout` 个人 Skill 已完成；已保存 70 分与 95 分对照图、空间分配案例和通用容器审计脚本，并通过真实概览页双视口实测与官方结构校验。

- [x] `SKILL-559` 提炼前后截图中真正改善观感的空间、字号、按钮和对齐原则。
- [x] `SKILL-560` 创建可自动发现的个人 Skill，并写入简洁、可执行的前端精修工作流。
- [x] `SKILL-561` 提供通用容器截图与几何审计脚本，支持 URL、视口和选择器参数。
- [x] `SKILL-562` 保存本次改动前后案例，完成 Skill 校验和脚本实测。

## 当前任务：其余一级页面容器级精修

> 当前行动：其余一级页面已按 `polish-ui-layout` 完成双视口整页与容器精修；同时修复资源配置仍跳转旧页面、弹窗演示数据与真实资源不一致、诊断详情裁剪和发布页超小字体等审计发现。

- [x] `POLISH-563` 生成其余一级页面 100%/125% 整页截图、一级容器截图和几何基线报告。
- [x] `POLISH-564` 精修卡带管理的卡片、操作区、列表密度和空状态。
- [x] `POLISH-565` 精修运行诊断的主从面板、详情分组、操作热区和内部滚动。
- [x] `POLISH-566` 精修资源中心及资源配置弹窗的分区、连接状态、表格和表单密度。
- [x] `POLISH-567` 精修打包发布的步骤、预检、目标选择、产物与历史区域。
- [x] `POLISH-568` 精修系统设置的分组层级、控件尺寸、说明文字和预览区域。
- [x] `POLISH-569` 完成真实数据、稀疏状态、100%/125% 双轴零溢出与关键交互回归。

## 当前任务：一级页面字号可读性专项

> 当前行动：一级页面字号专项已完成；普通界面文字最低为 12px，仅图表刻度、运行 ID、事件类型、节点键和时间戳等技术注释保留 11px，100%/125% 布局与真实交互回归均通过。

- [x] `TYPE-570` 审计概览、卡带管理、运行诊断、资源中心、资源弹窗、打包发布和系统设置的可见计算字号。
- [x] `TYPE-571` 修复普通正文、表格、状态、按钮和说明文字中低于 12px 的项目。
- [x] `TYPE-572` 复核保留为 11px 的技术注释是否清晰、受控且具备完整值访问方式。
- [x] `TYPE-573` 完成双视口截图、容器零溢出、真实数据和关键交互回归。

## 当前任务：运行诊断字号专项

> 当前行动：按用户要求暂停继续优化；已完成的字号与布局改动保留，后续在完整前端分支中继续验收。

- [ ] `DIAG-TYPE-574` 审计运行列表、选中运行、根因恢复、事件时间线、检查点和产物区域的字号与行高。
- [ ] `DIAG-TYPE-575` 放大卡带名、状态、节点、正文、操作说明和表格内容，仅保留少量次要时间与英文键为紧凑字号。
- [ ] `DIAG-TYPE-576` 调整列表列宽、行高和证据区空间，消除放大后的裁切与拥挤。
- [ ] `DIAG-TYPE-577` 完成 100%/125% 截图、容器双轴零溢出和真实诊断交互回归。

## 当前任务：项目数据清理与阶段快照

> 当前行动：按用户最新要求暂停删除，不再清理 `.data` 或项目文件；仅检查提交内容并创建当前完整状态的阶段性 Git 提交。

- [ ] `CLEAN-580` 停止仅属于当前仓库的服务与测试浏览器，核对并清空 `.data` 四区。
- [ ] `CLEAN-581` 删除未引用的失败试验目录、构建产物和语言缓存，不删除当前前端源码与本地开发运行时。
- [ ] `CLEAN-582` 更新文件清单与任务状态，执行源码语法、测试和前端生产构建。
- [ ] `CLEAN-583` 检查提交内容不含本机数据、密钥、日志与缓存，创建阶段性 Git 提交。

- [x] `REDESIGN-509` 在 `src/frontend-new` 建立独立工程，不导入旧前端的页面、组件、CSS 或路由。
- [x] `REDESIGN-510` 按稿件坐标重做概览页骨架：顶部统计、运行活动、待处理事项、底座能力、运行诊断入口和本地资源必须在首屏完整可见。
- [ ] `REDESIGN-511` 将待处理事项改为真实 `TODO.md` 数据，支持查看指定行、Markdown 预览和完整文件浏览，且空状态保持容器完整。
- [x] `REDESIGN-512` 完成新工程 100%/110%/125% 截图与双轴零溢出验收，确认概览页可用后再停下评分。
- [x] `REDESIGN-513` 在独立 HTML 工程完成 Flow 管理与空状态页面，对照两张 Img2 稿件并实现页面切换。
- [x] `REDESIGN-514` 完成运行诊断页面及选中运行、筛选、诊断详情和展开状态。
- [x] `REDESIGN-515` 完成资源中心与资源配置弹窗，明确模型连接、工具连接和本机凭据层级。
- [x] `REDESIGN-516` 完成打包发布与系统设置页面，保留稿件结构和完整控件状态。
- [x] `REDESIGN-517` 对全部独立页面执行 100%/125% 真实视口、页面级和卡片级溢出审计，并统一修正缓存与容器响应规则。
