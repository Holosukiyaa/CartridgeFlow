# CartridgeFlow TODO

> 本轮前端可用性、沙箱交互、工具模型收敛和卡带迁移报告已经完成。当前没有开放任务，下一轮工作由项目负责人重新评估和排序。

## 当前任务：v0.2.0 补发与 v0.3.0 发布

> 当前行动：REL-004，为已验证的发布提交创建 v0.3.0 标签，并推送 main 与标签。

- [ ] `REL-004` 提交当前改动、创建 v0.3.0 标签并推送分支与标签。
- [ ] `REL-005` 创建 v0.3.0 GitHub Release，并核验远端结果。

## 已冻结队列

原有 `CORE-001`、`PKG-001`、`UI-001`、`ART-001`、`TEST-001`、`REF-001`、`RELEASE-001` 和 `DIST-001` 暂停执行。任务内容由 Git 历史保存，重新评估前不作为当前待处理项展示。

## 维护规则

- 开放任务总数不超过 12 项；超过时先合并、排序或移除，而不是继续堆积。
- 只记录跨模块完整能力、发布阻断或高风险可靠性问题。
- 局部样式、文件整理、命名调整、普通重构和单条测试不单独进入 TODO。
- 每项任务必须说明结果和验收边界，不能只写“优化”“完善”或实现步骤。
- 任务完成后直接移出本文件；提交记录、自动报告和 GitHub 负责保存历史。
