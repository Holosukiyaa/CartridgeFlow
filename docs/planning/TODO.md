# CartridgeFlowLite TODO

## P0 - 个人产品交付闭环

- [ ] `PRODUCT-001` 定义 `Product Delivery Contract v1`：将卡带的主产物、附件、质量门槛、交付状态、revision 与可复现运行证据收敛为协议和运行时事实；运行成功不再等同于产品已交付。
- [ ] `PRODUCT-002` 实现产品运行记录与产物视图：同一次运行的主产物、附件、摘要、来源卡带版本、输入版本、Runner、耗时、失败/重试和下载记录必须可追溯；“再次运行”创建新 revision，不能覆盖既有交付物。
- [ ] `PRODUCT-003` 实现个人版本地/云端 Runner 放置策略：用户可看见数据去向、外部调用与成本归属；本地资源和敏感输入默认不因云端运行而被静默上传。
- [ ] `DIST-001` 定义个人运行台 `Cartridge Release Envelope v1`：冻结市场发行包、公开体验/交付合同、内容摘要、发布者签名、Runner 兼容性、权限和依赖重绑定边界；不得将开发工程 ZIP 或本机绑定状态直接作为市场发行物。

## P0 - 编排语义基线

- [ ] `ORCH-001` 固化 `ExecutionPlan v1`：定义 `sequence`、`fork`、`join(all/any/keyed)`、`loop`、`batch`、`wait` 和 `failure` 的可执行语义，清理当前画布、协议分析和运行器之间不一致的 `action_route`、`failure_route`、循环与合流行为；验收见 [n8n 编排取经与差异化报告](../architecture/N8N_ORCHESTRATION_BENCHMARK_REPORT.md)。

## P1 - 当前主线

- [ ] `ENG-021` 实施工程视图资源化表达与节点信息层级，覆盖外部 MCP 连接详情、资源画布节点、节点类别标识、可拖拽资源和按内容自适应的卡片布局；验收要求见 [工程视图资源化任务书](./ENGINEERING_VIEW_RESOURCE_TASK_BRIEF.md)。
- [ ] `ORCH-002` 以编译产物 `ExecutionPlan` 和运行期 token 替换一次性队列调度；支持可恢复的多次节点执行、确定性调度与 checkpoint 对齐。
- [ ] `ORCH-003` 建立类型化端口和值引用模型，记录 schema、producer、lineage、digest 和敏感级别；限制共享 `store` 只作为运行缓存，不能再承担隐式数据流语义。
- [ ] `ORCH-004` 实现卡带内子 Flow：固定 `id + version + interface digest`，具备显式输入/输出、effect 与权限契约，禁止 URL、任意 JSON 或跨卡带动态加载。
- [ ] `ORCH-005` 将 Probe/TestBench 升级为 fixtures、局部执行依赖闭包、脱敏输出快照、产物/副作用断言和可控回放。
- [ ] `ORCH-006` 把正常、失败、补偿和人工恢复线路统一投影到画布与运行 trace；每条副作用线路必须显示权限、重试、确认和回滚边界。
- [ ] `DIST-002` 实现开发台 Release Builder：以 `package/publish` 分析、卫生、可移植性和认证证据构建可验证发行包；发行包不携带密钥、私有 URL、本机路径、开发日志、运行时 Store、检查点或用户产物。
- [ ] `DIST-003` 实现运行台安装器：暂存下载/导入、摘要和签名校验、兼容性/能力预检、用户资源重绑定、原子激活、版本并存、回滚和卸载保留用户产物。
- [ ] `DIST-004` 实现市场桥接最小闭环：市场条目、不可变 Release、下载授权、安装来源和更新检查分离；第一版支持免费精选卡带和离线手动导入，不以支付系统阻断本地运行。

## P2 - 探针与适配能力预研

- [ ] `ADAPT-001` 定义授权范围内的接口事实采集模型：支持导入接口文档、样本、调用日志和 fixture，记录证据来源、置信度、脱敏策略与人工确认；不实施未经授权的主动扫描。
- [ ] `ADAPT-002` 定义可版本化的适配合同：以稳定输入/输出、字段映射、鉴权、超时、幂等、错误映射、effect 和回放边界描述新旧接口之间的转换；先作为个人开发者的本地适配卡带验证，不引入企业版控制台或多租户治理。

## 已完成

- [x] `DOC-001` 移除已拆分至独立工程的企业版架构文档，并同步清理文档入口与文件清单中的失效引用。
- [x] `DOC-002` 移除已完成、已失效的性能计划、安全审计记录和 v0.3 AI 开发者指南，并清理关联入口、治理检查与卫生测试。
- [x] `DOC-003` 重构 worker 协作指南与 mentor Skill：命令可从任意目录执行、提示词内嵌交付约束与报告格式、全部交付完成后受控清理 worktree。
- [x] `DOC-004` 恢复并更新 worker-004 的可直接执行启动命令，使用当前基线与内嵌交付报告模板。
