{
  "schema_version": "0.1",
  "profiles": [
    {
      "id": "release_envelope_validation",
      "description": "Validate the public CF-CRE@1 envelope, payload hashes, paths, and contract boundary."
    },
    {
      "id": "release_envelope_runtime",
      "description": "Verify Ed25519 publisher signatures, consult a local trust store, stage a verified payload, and expose activation readiness."
    }
  ]
}
