# CartridgeFlow 配方工作台与调优版本计划

更新时间：2026-08-03
状态：实现、浏览器验收与人工提交前审查完成

## 1. 产品边界

CartridgeFlow 不要求任何用户从空白手搭完整流程。AI 负责生成和修改全局流程轮廓；工作台是开发者与高级用户的调优、观测和发布界面；普通用户使用开发者已经调优并发布的配方产品。

工作台遵循三条展示规则：

1. 画布只把“配方步骤、可选说明、物料流向、条件分支”作为主信息，不把协议字段堆在节点卡片上。
2. 节点的执行器、模型、工具、数据契约、运行状态和协议事实统一进入详情卡片。
3. 左侧节点库提供已经组合好的配方步骤，不向用户暴露基础节点类型和内部默认配置。

用户可以移动节点、观察运行物料、配置资源、打包和发布；关键参数只能在单个节点内通过结构化控件调整。全局增删节点和拓扑修改仍由 AI 负责，不提供代码编辑作为正常工作流。

## 2. 协议分层

### CF-FARP@1.1

- 完整、可独立阅读和验证的 Root Flow 协议版本，不通过引用历史版本补齐规范正文。
- 基座契约升级为 `CARTRIDGEFLOW-BASE@0.3`。
- 显式声明并信任必需的内部子协议 `CF-TUNING@1.0`。
- 继续使用稳定执行适配器 `cf-farp.execution-plan.v1`，避免把调优版本与拓扑执行语义混为一层。

### CF-TUNING@1.0

- 只允许节点局部的效果参数补丁，不允许修改 Root Flow 拓扑、节点身份或执行计划。
- 修订内容寻址、不可变；节点头采用预期版本检查，冲突时拒绝覆盖。
- 发布生成不可变配方快照，支持激活历史版本和回滚。
- 发布包只携带当前发布快照；开发修订历史保存在卡带外的本机数据目录。
- 禁止在调优仓库中保存密钥、凭证、本机绝对路径或任意代码。

## 3. 基座与运行时

- [x] Base 0.3 注册并验证受信任子协议及运行适配器。
- [x] 保留 Base 0.2 和 CF-FARP 1.0 的已支持兼容声明，旧卡带不被默认升级。
- [x] 开发卡带默认使用 CF-FARP 1.1、Base 0.3 和 CF-TUNING 1.0。
- [x] 开发预览物化节点草稿头；安装、打包和生产运行只物化已发布快照。
- [x] 运行记录写入配方版本、修订映射和物化摘要，保证结果可追溯。
- [x] 打包前重新执行兼容性与发布快照校验，缺失、过期或篡改时 fail closed。

## 4. 工作台

- [x] 节点卡片收敛为配方名称、可选说明和输入到输出的物料关系。
- [x] 连接线显示明确的配方或物料流向，条件与失败路线保留可辨识状态。
- [x] 详细技术事实集中到右侧详情卡片。
- [x] 局部编辑只开放名称、说明、提示词、模型参数和超时等关键参数。
- [x] 左侧节点库改为配方步骤，隐藏基础节点类别和内部预设配置。
- [x] 顶部加入草稿头数量、发布版本、发布、激活与回滚控制。
- [x] 画布布局变化不改变配方效果摘要，也不制造新的调优版本。
- [x] 节点详情提供普通用户的等待、运行、操作、完成与失败状态预览。
- [x] 开发者可配置用户呈现、输入或审核操作、实时物料、结果恢复和安全开放参数。
- [x] 普通用户体验随节点调优版本保存，并提供覆盖全部 13 个配方步骤的就绪统计。

## 5. 质量门禁

- [x] 调优协议单元测试：不可变修订、并发头冲突、敏感值拒绝、发布、回滚、篡改与过期拒绝。
- [x] CF-FARP 1.1、Base 0.3、受信任子协议和打包目标契约测试。
- [x] 全量协议与运行时 conformance 通过（405 项通过，1 项跳过）。
- [x] 前端类型检查、静态断言、生产构建和 Python 编译通过。
- [x] 真实浏览器完成节点调优、发布两个版本、回滚、配方画布和窄视口回归，控制台错误为 0。
- [x] 完成人工提交前审查并修复统计遗漏与重复物料展示；结构化 AutoReview 已尝试，但因本机缺少强制依赖 TruffleHog 在模型调用前 fail closed，未将其误报为通过。

## 6. 验收结论

当前实现已满足：Root Flow 仍是唯一的全局编排事实；内部调优层有独立协议和版本历史；普通产品界面只消费已发布配方；开发者在当前工作台中看到清晰配方流、完整详情、普通用户投影和局部关键参数，并能可靠地发布或回滚效果版本。
