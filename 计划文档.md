# CartridgeFlow 无 AI 搭建体验改造计划

更新时间：2026-08-02
状态：执行中

## 1. 目标

让用户在不依赖 AI 辅助搭建的情况下，也能独立完成流程创建、配置、校验、运行、排错和交付。AI 只能用于提速和生成建议，不能成为完成核心任务的前置条件。

本计划以四类用户为验收对象：

| 用户 | 默认工作方式 | 核心诉求 | 最低体验目标 |
|---|---|---|---|
| 普通创作者 | 引导模式、模板、业务表单 | 看得懂、点得动、无需协议知识 | 8/10 |
| 高级搭建者 | 标准画布、可组合节点 | 高效编排、快速复用、清楚排错 | 8/10 |
| 协议工程师 | 工程模式、完整契约视图 | 精确控制、可验证、可迁移 | 8/10 |
| 运行与审核人员 | 就绪检查、运行记录、交付 | 风险明确、结果可追溯、恢复直接 | 8/10 |

“8/10”不以主观观感单独判定，必须同时满足第 4 节中的任务指标和质量门禁。

## 2. 产品原则

1. 业务语义优先。普通用户只处理“输入内容、生成结果、人工确认、条件分支”等概念，不暴露节点 ID、边类型、角色名或原始 JSON。
2. 一次配置形成完整事实。新增节点时，画布节点、清单输入、模型角色和运行参数必须原子写入，避免“看起来已配置、运行时未生效”。
3. 单一就绪结论。设计、模型、工具、运行和打包使用同一份可执行就绪报告，不允许不同页面互相矛盾。
4. 错误必须可行动。每个阻塞项都说明影响、定位对象和下一步，并能一键进入正确配置位置。
5. 渐进式复杂度。默认展示引导模式；标准和工程模式按需进入，能力不被删减。
6. 模板可编辑、可解释。模板负责缩短起步时间，但生成的仍是普通可编辑 Flow，不形成黑盒。
7. AI 可选。关闭 AI 或没有搭建助手时，所有核心搭建路径仍能完成；运行节点所需的业务模型配置不属于“AI 辅助搭建”。

## 3. 黄金路径

第一阶段必须覆盖并持续回归以下四条路径：

| 路径 | 用户结果 | 无 AI 搭建要求 |
|---|---|---|
| 主题或输入 -> AI 生成 -> Markdown | 将输入生成结构化内容 | 点击添加节点，表单配置输入、提示词、输出和模型，一次检查后运行 |
| 读取文件 -> 摘要 -> 写入文件 | 完成本地文件处理 | 使用文件选择和权限提示，不手写工具参数或资源 URI |
| 生成 -> 人工审核 -> 修改或交付 | 建立人在回路流程 | 审核节点提供批准、退回和修改目标的结构化配置 |
| 条件 -> 分支 -> 合并 | 按业务条件执行不同路径 | 条件使用可视规则编辑器，分支状态和合并要求可见 |

## 4. 8 分验收标准

每一类用户分别执行适用黄金路径并评分，任何一类低于 8 分都不能视为完成。

### 4.1 可量化指标

- 普通创作者的基础路径从空白到首次可运行不超过 8 分钟。
- 高级搭建者的分支或人工审核路径从空白到首次可运行不超过 15 分钟。
- 首次独立完成率不低于 85%；经过页面内提示恢复后完成率不低于 95%。
- 普通创作者核心路径中，原始 JSON、节点 ID、协议字段和模型角色名的暴露次数为 0。
- “已就绪但实际无法运行”的假绿色状态为 0。
- 从失败信息到定位并完成修复不超过 2 分钟，且不要求查看服务端日志。
- 所有新增控件在桌面常用视口、移动窄视口及 100%/125% 缩放下无重叠、截断或不可达操作。
- 键盘可完成节点添加、配置、就绪项跳转和运行；表单标签、焦点与错误提示可被辅助技术识别。

### 4.2 评分维度

每类用户按以下五项加权评分：任务完成 35%、理解成本 20%、操作效率 20%、错误恢复 15%、信任与可预期性 10%。其中任务完成或信任低于 7 分时，即使总分达到 8 分也判定不通过。

## 5. 实施阶段

### P0：事实一致性与假就绪清零

目标：先保证界面所见、保存内容、就绪结论和运行行为一致。

- [x] 新增节点时原子写入完整业务配置，不再先创建空节点再二次更新。
- [x] 输入节点同步维护清单输入契约，字段使用稳定键和业务标签。
- [x] 生成类预设编译真实提示词、输出名和模型角色，不再被通用占位提示覆盖。
- [x] 修复无模型角色声明时 AI 节点被误判为就绪的问题。
- [x] 建立统一创作就绪 API，并在运行前阻止已知不可执行 Flow。
- [x] 将工具声明与本机资源可用性并入同一就绪报告。
- [ ] 将交付契约和打包前检查并入同一就绪报告。
- [ ] 为已存在的旧 Flow 增加兼容迁移和明确诊断。

### P1：普通用户可完成的确定性搭建

目标：不懂协议和内部标识的用户也能完成基础路径。

- [x] 节点库支持点击选择、配置后添加，同时保留拖拽操作。
- [x] 运行输入展示业务标签，不展示稳定键。
- [x] 模型配置改为一次选择同时完成 Flow 角色与节点绑定，隐藏普通用户不需要的三层概念。
- [x] 输入节点改为结构化字段编辑器，支持名称、类型、必填、默认值、排序和删除。
- [ ] AI 节点提供提示词、输出、模型、失败处理的业务表单和即时预览。
- [x] 读取文件节点提供文件选择器，生成真实只读工具调用并通过统一就绪检查。
- [ ] 写入文件节点补齐权限范围、覆盖策略和示例结果。
- [x] 人工审核节点提供结构化审核要求，暂停后确认通过并继续。
- [ ] 条件节点、退回修改、超时和修改目标需要在支持动态选择路由的新协议版本中实现。
- [x] 点击新增节点后按当前卡片尺寸自动布局，避免旧坐标造成节点重叠。
- [ ] 节点连线提供合法目标提示和常用后续节点快捷添加。

### P2：引导模式、模板和恢复路径

目标：减少空白画布决策，并使错误恢复无需离开当前任务。

- [x] 默认进入业务视图，工程视图可随时切换；业务视图只展示当前协议可兑现的节点能力。
- [ ] 提供四条黄金路径模板及空白 Flow，创建前展示输入、输出和所需资源。
- [ ] 统一就绪面板按“必须处理、建议改进、已完成”分组，并提供直接修复动作。
- [ ] 保存、校验和运行失败保留用户输入，支持重试、撤销和回到问题节点。
- [ ] 对危险或不可逆操作给出影响范围与确认，普通编辑保持即时保存。

### P3：运行、审核与交付闭环

目标：运行人员无需理解搭建细节，也能判断风险和处理失败。

- [ ] 运行前只收集当前 Flow 真正需要的输入，并展示数据去向和权限。
- [ ] 运行视图以业务步骤展示当前状态、耗时、重试和产物，不以内部事件为主界面。
- [ ] 失败信息提供原因、受影响步骤、可重试性和具体修复入口。
- [ ] 人工审核支持待办、上下文对比、批准、退回和审计记录。
- [ ] 交付前复用统一就绪报告，并验证至少一次成功运行及产物契约。

### P4：验证、治理与发布

目标：把 8 分体验变成可持续门禁，而不是一次性改版。

- [ ] 为四条黄金路径补齐 API、运行时、前端组件和浏览器端到端测试。
- [ ] 建立四类用户的任务脚本、观察表、评分表和问题分级规则。
- [ ] 每轮至少 5 名普通创作者、3 名高级搭建者、2 名协议工程师、2 名运行或审核人员参与可用性验证。
- [ ] 发布门禁自动检查完成率、假就绪、浏览器错误、可访问性和关键路径耗时。
- [ ] 完成代码审查、迁移说明、回滚方案和阶段提交。

## 6. 当前进度与下一步

截至 2026-08-02，P0 主链以及 P1 的点击添加、业务输入标签、一键模型绑定、结构化输入、读取文件、人工确认、工具资源就绪检查和点击新增自动布局已经实现。结构化字段的内部键在改名和换序后保持稳定；新增或编辑节点只替换本节点拥有的运行输入，并按角色 ID 合并模型声明，不会删除其他节点或高级角色的契约。业务视图已收回会生成无效流程的扫描项目、导入日志、拆分、伪文件交付、伪条件分支和伪退回入口；高级资产、远程和自定义节点只在工程视图显示。390 项完整一致性测试、前端类型检查、静态断言、生产构建、Python 编译、本地代码审查以及桌面、125% 缩放、390px 窄视口真实浏览器回归均已通过，阶段成果已准备提交。

当前不能宣称四类用户均已达到 8 分。第一条基础生成路径和“读取文件”搭建路径已具备进入正式用户测试的条件；实际分数仍需按第 4 节招募用户测量。`CF-FARP@1.0` 的 ExecutionPlan 只定义顺序、并行、合并、循环、批处理、等待和失败边，不定义按运行数据选择单一路径的动态条件边；人工审核退回任意前序节点也会被合法后继校验拒绝。发布版协议不可原地改变语义，因此条件分支与退回修改必须通过新协议版本、适配器、实现和一致性测试推进，不能在当前界面假装可用。

接下来的执行顺序：

1. 扩展就绪报告到工具、资源、交付和打包，并统一各入口状态。
2. 补齐 AI 节点和写入文件节点的业务表单、权限说明、失败策略与即时预览。
3. 完成基础生成、读取文件和人工确认三条当前协议可表达路径的运行、失败恢复与打包闭环。
4. 为动态条件路由和人工退回设计新协议版本，补齐适配器、运行时、安全校验和迁移策略后再恢复界面入口。
5. 增加黄金路径模板和任务内恢复入口，开展四类用户的正式可用性测试。
6. 执行完整自动化、真实浏览器桌面与窄视口回归、代码审查，修复后提交阶段成果。

## 7. 验证与发布门禁

每个阶段至少执行：

```powershell
python scripts/run_conformance.py --quiet
npm --prefix src/frontend run typecheck
npm --prefix src/frontend test
npm --prefix src/frontend run build
python -m compileall -q src scripts
```

此外必须通过真实浏览器回归：新建 Flow、点击添加节点、配置输入和模型、就绪项跳转、运行前门禁、执行、失败恢复、重新执行、查看产物和打包。浏览器控制台错误为 0，并保留关键视口截图作为阶段证据。

提交前执行独立代码审查，优先检查数据契约漂移、旧 Flow 兼容、竞态保存、假就绪、错误状态丢失和测试盲区。发现高、中风险问题必须先修复再提交。

## 8. 风险与控制

- 旧 Flow 可能依赖缺失或不一致的输入、模型配置。通过只增不破坏的读取兼容和显式迁移诊断控制风险。
- 单一就绪报告可能与运行时校验再次分叉。报告应复用运行时现有校验函数，并以契约测试锁定结果。
- 引导模式可能限制高级能力。引导、标准和工程模式共享同一源数据，只改变呈现与默认动作。
- 自动保存可能产生竞态。节点更新使用版本或请求序列保护，失败时保留本地草稿并明确显示未保存状态。
- 模板可能变成不可维护的特例。模板只生成协议合法的普通节点和边，不引入模板专属运行语义。

## 9. 初步资源与周期

在一名前端、一名后端、一名产品设计和一名测试协同投入下，完成 P0-P4 的初步估算为 8 至 12 周。优先级不是平均分配：先在 2 至 3 周内把第一条黄金路径稳定到 8 分，再逐条扩展并复用同一套表单、就绪和恢复基础设施。实际发布以第 4 节指标达标为准，不以日期替代验收。
